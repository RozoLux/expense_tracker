# Expense Tracker Project

## Overview
This Expense Tracker project is a simple database application that allows users to track their expenses. It includes a relational database schema, implemented in PostgreSQL, and a Python layer to interact with the database.

## Database Schema (ER Diagram)
expense_tracker.drawio


## Getting Started
### Prerequisites
- Python installed (version 3.9.9)
- PostgreSQL installed (version X.X.X)

### Installation
1. Clone the repository:
   ```bash
   git clone https://github.com/RozoLux/expense-tracker.git
   cd expense-tracker


pip install -r requirements.txt

psql -d expense_tracker -f setup.sql

